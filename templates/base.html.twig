<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TDBR - Teddy Bears Artisanaux{% endblock %}</title>
    <meta name="description" content="{% block description %}TDBR - Créations artisanales de peluches et teddy bears personnalisés{% endblock %}">

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('build/app.a3414627.css') }}">
    {% endblock %}
</head>
<body class="bg-white text-dark font-sans antialiased">
    {% include 'layout/navbar.html.twig' %}

    {# Bandeau site en construction #}
    <div class="bg-amber-50 border-b border-amber-200">
        <div class="container mx-auto px-4 py-3">
            <div class="flex flex-col sm:flex-row items-start sm:items-center gap-2 sm:gap-6 text-sm">
                <span class="font-semibold text-amber-800 flex-shrink-0">Site en cours de construction</span>
                <span class="text-amber-700">
                    Les articles et prix affichés sont réels, mais la commande en ligne n'est pas encore disponible.
                </span>
                <div class="flex flex-wrap gap-3 flex-shrink-0">
                    <a href="{{ path('contact') }}" class="font-medium text-amber-900 underline underline-offset-2 hover:text-amber-700">Nous contacter</a>
                    <a href="{{ path('devis') }}" class="font-medium text-amber-900 underline underline-offset-2 hover:text-amber-700">Faire un devis</a>
                    <a href="{{ path('avis_liste') }}" class="font-medium text-amber-900 underline underline-offset-2 hover:text-amber-700">Laisser un avis</a>
                </div>
            </div>
        </div>
    </div>

    {% include 'layout/flash_messages.html.twig' %}

    <main class="min-h-screen">
        {% block body %}{% endblock %}
    </main>

    {% include 'layout/footer.html.twig' %}

    {% block javascripts %}
        <script src="{{ asset('build/runtime.f073a35a.js') }}"></script>
        <script src="{{ asset('build/705.31b65463.js') }}"></script>
        <script src="{{ asset('build/app.579e6744.js') }}"></script>
    {% endblock %}
</body>
</html>
