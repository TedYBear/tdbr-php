<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TDBR - Teddy Bears Artisanaux{% endblock %}</title>
    <meta name="description" content="{% block description %}TDBR - Créations artisanales de peluches et teddy bears personnalisés{% endblock %}">

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('build/app.a3414627.css') }}">
    {% endblock %}
</head>
<body class="bg-white text-dark font-sans antialiased">
    {% include 'layout/navbar.html.twig' %}

    {# Bandeau site en construction #}
    <div class="bg-amber-500 text-white">
        <div class="container mx-auto px-4 py-2.5">
            <div class="flex flex-col sm:flex-row items-start sm:items-center gap-1.5 sm:gap-6 text-sm">
                <span class="font-bold flex-shrink-0 uppercase tracking-wide text-xs">Site en construction</span>
                <span class="opacity-90">
                    Les articles et prix sont réels — la commande en ligne n'est pas encore disponible.
                </span>
                <div class="flex flex-wrap gap-4 flex-shrink-0 sm:ml-auto">
                    <a href="{{ path('contact') }}" class="font-semibold underline underline-offset-2 hover:opacity-75 transition">Nous contacter</a>
                    <a href="{{ path('devis') }}" class="font-semibold underline underline-offset-2 hover:opacity-75 transition">Faire un devis</a>
                    <a href="{{ path('avis_liste') }}" class="font-semibold underline underline-offset-2 hover:opacity-75 transition">Laisser un avis</a>
                </div>
            </div>
        </div>
    </div>

    {% include 'layout/flash_messages.html.twig' %}

    <main class="min-h-screen">
        {% block body %}{% endblock %}
    </main>

    {% include 'layout/footer.html.twig' %}

    {% block javascripts %}
        <script src="{{ asset('build/runtime.f073a35a.js') }}"></script>
        <script src="{{ asset('build/705.31b65463.js') }}"></script>
        <script src="{{ asset('build/app.579e6744.js') }}"></script>
    {% endblock %}
</body>
</html>
