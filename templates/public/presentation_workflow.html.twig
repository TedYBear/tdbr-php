{% extends 'base.html.twig' %}

{% block title %}Ma façon de travailler - TDBR{% endblock %}

{% block body %}
    {% include 'public/_presentation_subnav.html.twig' %}

    <div class="min-h-screen bg-gradient-to-b from-gray-50 to-white py-12 md:py-20">
        <div class="container mx-auto px-4 max-w-4xl">

            {# Header #}
            <div class="text-center mb-12 md:mb-16">
                <h1 class="font-heading text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 mb-4">
                    Ma façon de travailler
                </h1>
                <p class="text-lg text-gray-600 max-w-2xl mx-auto">
                    Du concept à la création — les outils et méthodes derrière chaque production TDBR.
                </p>
            </div>

            {# Contenu à venir #}
            <div class="space-y-8">

                {# Bloc flux de travail #}
                <div class="card p-8 md:p-10">
                    <div class="flex items-center gap-4 mb-6">
                        <div class="w-12 h-12 bg-gradient-to-br from-primary to-secondary rounded-xl flex items-center justify-center text-white text-2xl flex-shrink-0">
                            ⚙️
                        </div>
                        <h2 class="font-heading text-2xl md:text-3xl font-bold text-gray-900">
                            Mon flux de travail
                        </h2>
                    </div>
                    <div class="p-6 bg-accent/40 rounded-xl border border-primary/20 text-center">
                        <p class="text-gray-500 italic">Contenu à venir — je décrirai ici les étapes de ma création, de l'idée initiale jusqu'au produit fini.</p>
                    </div>
                </div>

                {# Bloc outils #}
                <div class="card p-8 md:p-10">
                    <div class="flex items-center gap-4 mb-6">
                        <div class="w-12 h-12 bg-gradient-to-br from-primary to-secondary rounded-xl flex items-center justify-center text-white text-2xl flex-shrink-0">
                            🛠️
                        </div>
                        <h2 class="font-heading text-2xl md:text-3xl font-bold text-gray-900">
                            Les outils utilisés
                        </h2>
                    </div>
                    <div class="p-6 bg-accent/40 rounded-xl border border-primary/20 text-center">
                        <p class="text-gray-500 italic">Contenu à venir — logiciels, IA, matériel... tout ce qui compose ma boîte à outils créative.</p>
                    </div>
                </div>

                {# Bloc impression / production #}
                <div class="card p-8 md:p-10">
                    <div class="flex items-center gap-4 mb-6">
                        <div class="w-12 h-12 bg-gradient-to-br from-primary to-secondary rounded-xl flex items-center justify-center text-white text-2xl flex-shrink-0">
                            🖨️
                        </div>
                        <h2 class="font-heading text-2xl md:text-3xl font-bold text-gray-900">
                            Production & impression
                        </h2>
                    </div>
                    <div class="p-6 bg-accent/40 rounded-xl border border-primary/20 text-center">
                        <p class="text-gray-500 italic">Contenu à venir — comment je passe du design numérique au produit physique.</p>
                    </div>
                </div>

            </div>

            {# Navigation entre pages #}
            <div class="mt-16 flex flex-col sm:flex-row gap-4 justify-between">
                <a href="{{ path('presentation') }}" class="btn-outline px-6 py-3 flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                    </svg>
                    À propos
                </a>
                <a href="{{ path('presentation_partenaires') }}" class="btn-primary px-6 py-3 flex items-center gap-2">
                    Mes partenaires
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                </a>
            </div>
        </div>
    </div>
{% endblock %}
