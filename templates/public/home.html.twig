{% extends 'base.html.twig' %}

{% block title %}TDBR - Goodies Personnalisés{% endblock %}

{% block body %}
    {# Hero Section #}
    <section class="relative min-h-[90vh] flex items-center justify-center overflow-hidden">
        {# Background Image #}
        <div class="absolute inset-0 z-0">
            <img src="{{ asset('build/images/main_page.png') }}"
                 alt="TDBR - Goodies Personnalisés"
                 class="w-full h-full object-cover">
            <div class="absolute inset-0 bg-black/40"></div>
        </div>

        {# Content #}
        <div class="container mx-auto px-4 text-center relative z-10" style="animation: fadeIn 1s ease forwards;">
            <img src="{{ asset('uploads/general/tdbr-logo.png') }}"
                 alt="TDBR"
                 class="h-48 md:h-64 mx-auto mb-8 md:mb-12">
            <h1 class="font-heading text-4xl sm:text-5xl md:text-6xl lg:text-8xl font-bold mb-4 md:mb-6 tracking-tight text-white">
                Bienvenue chez <span class="gradient-text">TDBR</span>
            </h1>
            <p class="text-base sm:text-lg md:text-xl lg:text-2xl mb-6 md:mb-8 font-light max-w-2xl mx-auto text-white">
                Goodies personnalisés qui racontent votre histoire
            </p>
            <div class="flex flex-col sm:flex-row gap-3 md:gap-4 justify-center">
                <a href="{{ path('catalogue') }}" class="btn-primary px-6 md:px-8 py-3 md:py-4 text-base md:text-lg">
                    Découvrir le Catalogue
                </a>
                <a href="{{ path('contact') }}" class="btn-secondary px-6 md:px-8 py-3 md:py-4 bg-white/90 backdrop-blur-sm text-gray-900 hover:bg-white text-base md:text-lg">
                    Demander un Devis
                </a>
            </div>
        </div>

        {# Scroll indicator - hidden on mobile #}
        <div class="hidden md:block absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
            <svg class="w-6 h-6 text-white/70" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
            </svg>
        </div>
    </section>

    {# Features Section #}
    <section class="py-12 md:py-20 bg-gradient-to-b from-white to-gray-50">
        <div class="container mx-auto px-4">
            <div class="text-center mb-10 md:mb-16">
                <h2 class="font-heading text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 mb-3 md:mb-4">
                    Pourquoi choisir <span class="gradient-text">TDBR</span> ?
                </h2>
                <p class="text-base md:text-lg text-gray-600 max-w-2xl mx-auto">
                    Des créations uniques pour vos goodies personnalisés
                </p>
            </div>

            <div class="grid md:grid-cols-3 gap-6 md:gap-8 max-w-6xl mx-auto">
                {# Feature 1 #}
                <div class="card hover-lift p-6 md:p-8 text-center group">
                    <div class="w-14 h-14 md:w-16 md:h-16 mx-auto mb-4 md:mb-6 bg-gradient-to-br from-primary to-secondary rounded-2xl flex items-center justify-center text-white text-2xl md:text-3xl group-hover:scale-110 transition-smooth">
                        🎨
                    </div>
                    <h3 class="font-heading text-xl md:text-2xl font-semibold text-gray-900 mb-3 md:mb-4">
                        Designs Originaux
                    </h3>
                    <p class="text-sm md:text-base text-gray-600 leading-relaxed">
                        Des créations uniques inspirées du street art et de vos passions : jeux de société, fromage, et bien plus.
                    </p>
                </div>

                {# Feature 2 #}
                <div class="card hover-lift p-6 md:p-8 text-center group">
                    <div class="w-14 h-14 md:w-16 md:h-16 mx-auto mb-4 md:mb-6 bg-gradient-to-br from-primary to-secondary rounded-2xl flex items-center justify-center text-white text-2xl md:text-3xl group-hover:scale-110 transition-smooth">
                        ⚡
                    </div>
                    <h3 class="font-heading text-xl md:text-2xl font-semibold text-gray-900 mb-3 md:mb-4">
                        Qualité Premium
                    </h3>
                    <p class="text-sm md:text-base text-gray-600 leading-relaxed">
                        Des supports de qualité pour des goodies qui durent : mugs, t-shirts, tote bags, et accessoires.
                    </p>
                </div>

                {# Feature 3 #}
                <div class="card hover-lift p-6 md:p-8 text-center group">
                    <div class="w-14 h-14 md:w-16 md:h-16 mx-auto mb-4 md:mb-6 bg-gradient-to-br from-primary to-secondary rounded-2xl flex items-center justify-center text-white text-2xl md:text-3xl group-hover:scale-110 transition-smooth">
                        💡
                    </div>
                    <h3 class="font-heading text-xl md:text-2xl font-semibold text-gray-900 mb-3 md:mb-4">
                        Sur Mesure
                    </h3>
                    <p class="text-sm md:text-base text-gray-600 leading-relaxed">
                        Vous avez un projet ? On le concrétise ensemble avec un service de personnalisation complet.
                    </p>
                </div>
            </div>
        </div>
    </section>

    {# CTA Section #}
    <section class="py-12 md:py-20 bg-gradient-to-br from-gray-900 via-primary to-gray-900 text-white relative overflow-hidden">
        <div class="absolute inset-0 opacity-10">
            <div class="absolute top-0 left-0 w-96 h-96 bg-secondary rounded-full filter blur-3xl"></div>
            <div class="absolute bottom-0 right-0 w-96 h-96 bg-secondary rounded-full filter blur-3xl"></div>
        </div>

        <div class="container mx-auto px-4 text-center relative z-10">
            <h2 class="font-heading text-3xl md:text-4xl lg:text-5xl font-bold mb-4 md:mb-6">
                Prêt à créer vos goodies ?
            </h2>
            <p class="text-base md:text-lg lg:text-xl text-gray-300 mb-8 md:mb-10 max-w-2xl mx-auto leading-relaxed">
                Que ce soit pour un événement, un cadeau d'entreprise, ou simplement pour vous faire plaisir,
                contactez-moi pour un devis personnalisé.
            </p>
            <div class="flex flex-col sm:flex-row gap-3 md:gap-4 justify-center">
                <a href="{{ path('contact') }}" class="btn-primary px-6 md:px-8 py-3 md:py-4 text-base md:text-lg">
                    Demander un Devis
                </a>
                <a href="{{ path('presentation') }}" class="px-6 md:px-8 py-3 md:py-4 border-2 border-white text-white font-semibold rounded-xl hover:bg-white hover:text-gray-900 transition-smooth text-base md:text-lg">
                    En savoir plus
                </a>
            </div>
        </div>
    </section>

    {# Contact Banner #}
    <section class="py-10 md:py-16 bg-gradient-to-b from-gray-50 to-white">
        <div class="container mx-auto px-4">
            <div class="max-w-4xl mx-auto card-glass p-6 md:p-12 text-center">
                <h3 class="font-heading text-2xl md:text-3xl lg:text-4xl font-bold text-gray-900 mb-3 md:mb-4">
                    Une question ? Un projet ?
                </h3>
                <p class="text-base md:text-lg text-gray-600 mb-6 md:mb-8">
                    N'hésitez pas à me contacter, je serai ravi d'échanger avec vous sur votre projet.
                </p>
                <a href="{{ path('contact') }}" class="btn-primary inline-block px-6 md:px-8 py-3 md:py-4 text-base md:text-lg">
                    Nous Contacter
                </a>
            </div>
        </div>
    </section>
{% endblock %}
