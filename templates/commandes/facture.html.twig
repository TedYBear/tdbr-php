<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Facture {{ commande.numero }} — TDBR</title>
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            font-size: 13px;
            color: #1a1a1a;
            background: #f0f0f0;
            line-height: 1.5;
        }

        /* Barre d'action — masquée à l'impression */
        .print-bar {
            background: #111827;
            color: #fff;
            padding: 0.75rem 1.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 1rem;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        .print-bar span { font-size: 0.875rem; opacity: 0.8; }
        .print-btn {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: #fff;
            color: #111827;
            border: none;
            padding: 0.5rem 1.25rem;
            border-radius: 0.5rem;
            font-weight: 700;
            font-size: 0.875rem;
            cursor: pointer;
        }
        .back-link {
            color: rgba(255,255,255,0.7);
            text-decoration: none;
            font-size: 0.875rem;
            display: flex;
            align-items: center;
            gap: 0.4rem;
        }
        .back-link:hover { color: #fff; }

        /* Page A4 */
        .page {
            max-width: 210mm;
            min-height: 297mm;
            margin: 2rem auto;
            background: #fff;
            padding: 15mm 18mm 12mm 18mm;
            box-shadow: 0 4px 24px rgba(0,0,0,0.12);
            display: flex;
            flex-direction: column;
        }

        /* En-tête */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding-bottom: 8mm;
            border-bottom: 2px solid #1a1a1a;
            margin-bottom: 8mm;
        }
        .brand-name {
            font-size: 28px;
            font-weight: 900;
            letter-spacing: -0.5px;
            color: #1a1a1a;
        }
        .brand-tagline {
            font-size: 11px;
            color: #666;
            margin-top: 2px;
        }
        .brand-info {
            font-size: 11px;
            color: #555;
            margin-top: 6px;
            line-height: 1.6;
        }
        .invoice-meta {
            text-align: right;
        }
        .invoice-label {
            font-size: 22px;
            font-weight: 800;
            color: #1a1a1a;
            letter-spacing: 1px;
            text-transform: uppercase;
        }
        .invoice-number {
            font-size: 14px;
            font-weight: 700;
            color: #555;
            margin-top: 4px;
        }
        .invoice-date {
            font-size: 12px;
            color: #777;
            margin-top: 2px;
        }

        /* Bloc adresses */
        .addresses {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 6mm;
            margin-bottom: 8mm;
        }
        .address-block {
            padding: 5mm;
            background: #f8f8f8;
            border-radius: 4px;
        }
        .address-label {
            font-size: 10px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            color: #999;
            margin-bottom: 3mm;
        }
        .address-name {
            font-size: 14px;
            font-weight: 700;
            margin-bottom: 1mm;
        }
        .address-detail {
            font-size: 12px;
            color: #555;
            line-height: 1.6;
        }

        /* Badge statut payé */
        .paid-badge {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            background: #dcfce7;
            color: #15803d;
            font-weight: 700;
            font-size: 11px;
            padding: 3px 8px;
            border-radius: 12px;
            margin-top: 4px;
        }

        /* Tableau articles */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 6mm;
        }
        thead tr {
            background: #1a1a1a;
            color: #fff;
        }
        thead th {
            padding: 3mm 4mm;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        thead th:first-child { text-align: left; border-radius: 3px 0 0 3px; }
        thead th:last-child  { text-align: right; border-radius: 0 3px 3px 0; }
        thead th.right { text-align: right; }
        thead th.center { text-align: center; }

        tbody tr { border-bottom: 1px solid #e5e5e5; }
        tbody tr:last-child { border-bottom: none; }
        tbody tr:nth-child(even) { background: #fafafa; }
        tbody td { padding: 3mm 4mm; font-size: 12px; vertical-align: top; }
        tbody td.right { text-align: right; }
        tbody td.center { text-align: center; }
        .item-name { font-weight: 600; }
        .item-variant { font-size: 11px; color: #777; margin-top: 1px; }

        /* Totaux */
        .totals {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 8mm;
        }
        .totals-box {
            width: 60mm;
        }
        .total-row {
            display: flex;
            justify-content: space-between;
            padding: 1.5mm 0;
            font-size: 12px;
            border-bottom: 1px solid #eee;
        }
        .total-row:last-child { border-bottom: none; }
        .total-row.discount { color: #15803d; font-weight: 600; }
        .total-row.grand {
            padding: 3mm 0 0;
            font-size: 15px;
            font-weight: 900;
            border-top: 2px solid #1a1a1a;
            margin-top: 1mm;
        }
        .total-label { color: #555; }
        .total-label.grand { color: #1a1a1a; }
        .total-value { font-weight: 700; }

        /* Paiement */
        .payment-info {
            background: #f8f8f8;
            border-left: 3px solid #1a1a1a;
            padding: 4mm 5mm;
            border-radius: 0 4px 4px 0;
            margin-bottom: 6mm;
            font-size: 12px;
        }
        .payment-info strong { display: block; font-size: 11px; text-transform: uppercase; letter-spacing: 0.5px; color: #888; margin-bottom: 1mm; }

        /* Mode livraison */
        .shipping-info {
            font-size: 12px;
            color: #555;
            margin-bottom: 6mm;
            padding: 3mm 5mm;
            border: 1px solid #e5e5e5;
            border-radius: 4px;
        }
        .shipping-info strong { color: #1a1a1a; }

        /* Footer */
        .footer {
            margin-top: auto;
            padding-top: 6mm;
            border-top: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            font-size: 10px;
            color: #999;
        }
        .footer-thanks {
            font-size: 13px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 1mm;
        }
        .footer-legal { line-height: 1.6; }

        /* Print */
        @media print {
            body { background: white; }
            .print-bar { display: none !important; }
            .page {
                margin: 0;
                padding: 12mm 15mm 10mm 15mm;
                box-shadow: none;
                min-height: auto;
            }
            @page { size: A4; margin: 0; }
        }
    </style>
</head>
<body>

    {# Barre d'action (masquée à l'impression) #}
    <div class="print-bar">
        <a href="javascript:history.back()" class="back-link">
            <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
            Retour
        </a>
        <span>Facture {{ commande.numero }}</span>
        <button class="print-btn" onclick="window.print()">
            <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z"/></svg>
            Imprimer / Sauvegarder PDF
        </button>
    </div>

    {# Page facture #}
    <div class="page">

        {# En-tête #}
        <div class="header">
            <div>
                <div class="brand-name">TDBR</div>
                <div class="brand-tagline">Teddy Bears Artisanaux</div>
                <div class="brand-info">
                    tedybear.fr<br>
                    contact@tedybear.fr<br>
                    64300 Orthez, France
                </div>
            </div>
            <div class="invoice-meta">
                <div class="invoice-label">Facture</div>
                <div class="invoice-number">N° {{ commande.numero }}</div>
                <div class="invoice-date">Date : {{ commande.createdAt|date('d/m/Y') }}</div>
                <div class="paid-badge">
                    <svg width="10" height="10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>
                    Payée
                </div>
            </div>
        </div>

        {# Adresses #}
        <div class="addresses">
            <div class="address-block">
                <div class="address-label">Facturer à</div>
                <div class="address-name">{{ commande.client.prenom }} {{ commande.client.nom }}</div>
                <div class="address-detail">
                    {{ commande.client.email }}<br>
                    {% if commande.client.telephone %}{{ commande.client.telephone }}{% endif %}
                </div>
            </div>
            <div class="address-block">
                <div class="address-label">Livrer à</div>
                {% set livraison = commande.modeLivraison %}
                {% if livraison and livraison.mode == 'relais' %}
                    <div class="address-name">Point relais</div>
                    <div class="address-detail">
                        {{ livraison.pointRelaisNom ?? '' }}<br>
                        {{ livraison.pointRelaisAdresse ?? '' }}
                    </div>
                {% else %}
                    {% set adresse = commande.adresseLivraison %}
                    <div class="address-name">{{ commande.client.prenom }} {{ commande.client.nom }}</div>
                    <div class="address-detail">
                        {{ adresse.adresse }}<br>
                        {% if adresse.complementAdresse %}{{ adresse.complementAdresse }}<br>{% endif %}
                        {{ adresse.codePostal }} {{ adresse.ville }}<br>
                        {{ adresse.pays == 'FR' ? 'France' : adresse.pays }}
                    </div>
                {% endif %}
            </div>
        </div>

        {# Tableau des articles #}
        {% set sousTotal = 0 %}
        <table>
            <thead>
                <tr>
                    <th>Désignation</th>
                    <th class="center">Qté</th>
                    <th class="right">Prix unit. TTC</th>
                    <th class="right">Sous-total</th>
                </tr>
            </thead>
            <tbody>
                {% for item in commande.articles %}
                    {% set ligneTotal = item.prix * item.quantity %}
                    {% set sousTotal = sousTotal + ligneTotal %}
                    <tr>
                        <td>
                            <div class="item-name">{{ item.nom }}</div>
                            {% if item.variant is defined and item.variant %}
                                <div class="item-variant">Variante : {{ item.variant.nom }}</div>
                            {% endif %}
                            {% if item.choices is defined and item.choices|length > 0 %}
                                {% for key, val in item.choices %}
                                    <div class="item-variant">{{ key }} : {{ val }}</div>
                                {% endfor %}
                            {% endif %}
                        </td>
                        <td class="center">{{ item.quantity }}</td>
                        <td class="right">{{ item.prix|number_format(2, ',', ' ') }} €</td>
                        <td class="right">{{ ligneTotal|number_format(2, ',', ' ') }} €</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        {# Totaux #}
        {% set fraisLivraison = commande.total - sousTotal + commande.reduction %}
        <div class="totals">
            <div class="totals-box">
                <div class="total-row">
                    <span class="total-label">Articles</span>
                    <span class="total-value">{{ sousTotal|number_format(2, ',', ' ') }} €</span>
                </div>
                <div class="total-row">
                    <span class="total-label">Livraison</span>
                    <span class="total-value">
                        {% if fraisLivraison <= 0 %}
                            Gratuite
                        {% else %}
                            {{ fraisLivraison|number_format(2, ',', ' ') }} €
                        {% endif %}
                    </span>
                </div>
                {% if commande.reduction > 0 %}
                    <div class="total-row discount">
                        <span>Réduction</span>
                        <span>- {{ commande.reduction|number_format(2, ',', ' ') }} €</span>
                    </div>
                {% endif %}
                <div class="total-row grand">
                    <span class="total-label grand">Total TTC</span>
                    <span>{{ commande.total|number_format(2, ',', ' ') }} €</span>
                </div>
            </div>
        </div>

        {# Paiement + Livraison #}
        <div class="payment-info">
            <strong>Paiement</strong>
            {% if commande.modePaiement == 'stripe' %}Carte bancaire (Stripe)
            {% elseif commande.modePaiement == 'virement' %}Virement bancaire
            {% elseif commande.modePaiement == 'paypal' %}PayPal
            {% else %}{{ commande.modePaiement|capitalize }}{% endif %}
            {% if commande.stripePaymentIntentId %}
                &nbsp;·&nbsp; Réf. {{ commande.stripePaymentIntentId }}
            {% endif %}
        </div>

        {% if commande.modeLivraison %}
            <div class="shipping-info">
                <strong>Mode de livraison :</strong> {{ commande.modeLivraison.label ?? commande.modeLivraison.mode }}
            </div>
        {% endif %}

        {# Notes #}
        {% if commande.notes %}
            <div class="shipping-info">
                <strong>Notes :</strong> {{ commande.notes }}
            </div>
        {% endif %}

        {# Footer #}
        <div class="footer">
            <div>
                <div class="footer-thanks">Merci pour votre commande !</div>
                <div class="footer-legal">
                    TDBR · tedybear.fr · contact@tedybear.fr<br>
                    Pour toute question : utilisez le formulaire de contact sur le site
                </div>
            </div>
            <div class="footer-legal" style="text-align:right;">
                Document généré le {{ "now"|date("d/m/Y") }}<br>
                Commande du {{ commande.createdAt|date('d/m/Y à H:i') }}
            </div>
        </div>

    </div>

</body>
</html>
