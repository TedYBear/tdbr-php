{# Champ CSRF pour les formulaires POST écrits à la main (hors composant Form Symfony).
   Validé côté contrôleur via isCsrfTokenValid('app', ...). #}
<input type="hidden" name="_token" value="{{ csrf_token('app') }}">
